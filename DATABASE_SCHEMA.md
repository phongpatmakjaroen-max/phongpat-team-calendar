# Database Schema

ไฟล์ติดตั้งจริงคือ `supabase_schema.sql`

| ตาราง | หน้าที่ |
| --- | --- |
| `profiles` | ชื่อผู้ใช้ สิทธิ์ admin/member สถานะอนุมัติและเปิดใช้งาน |
| `people` | รายชื่อคนในแผนกหรือผู้เกี่ยวข้อง แม้ยังไม่มีบัญชี |
| `events` | งาน รายการแจ้งข้อมูล วันหยุด ช่วงวันที่ สีหมุด และสถานะ |
| `event_people` | ความสัมพันธ์หลายคนต่อหนึ่งรายการ |
| `audit_logs` | ผู้กระทำ การเปลี่ยนแปลง และเวลา |

## Security

- เปิด Row Level Security ทุกตาราง
- ผู้ใช้ที่ยังไม่อนุมัติอ่านข้อมูลทีมไม่ได้
- สมาชิกที่อนุมัติแล้วอ่านและแก้ไขงานได้
- เฉพาะผู้ดูแลลบงานและจัดการสิทธิ์สมาชิกได้
- แอปใช้เฉพาะ anon/publishable key
- service-role/secret key ห้ามใช้ใน Streamlit

## Event rules

- `end_date` ต้องไม่น้อยกว่า `start_date`
- `item_type` มี `task`, `info`, `holiday`
- `info` และ `holiday` ไม่ใช้สถานะ `done`
- สีหมุดจำกัดไว้ 6 สีที่ UI รองรับ
